from __future__ import annotations

from mova.adapter.inbound.api.schemas.t import u
from mova.app.dtos.d import i, o
from mova.app.ports.input.S import e
from mova.app.ports.output.a import r


class c(e):
    def __init__(self, repository: r) -> None:
        self._repository = repository

    async def introduce_myself(self, schemas: u) -> o:
        return await self._repository.introduce_myself(i(
            id=schemas.id,
            name=schemas.name,
        ))